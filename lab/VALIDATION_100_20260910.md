# 100-node follow-through, 2026-09-10 — no completed measurement

Experiment `exp-05406ae40c07` has completed all 94 Public funding, CreateAccount
and Mint preparations. It has not produced a valid one-hour measurement.

## Runs and live checkpoint (11:50 UTC+08:00)

- `load-3ee85ef6d157`: failed during warmup on stale samples; all 14 Transfers
  subsequently confirmed with readiness. Diagnostic JSON/HTML/ZIP exported as
  `warmup-first-failed.*` in `lab/data/hour-100-durable.LVuLdv`; ZIP tested.
- `load-0b988267d898`: rolling monitor queue still exceeded freshness; all 14
  Transfers confirmed before the next controller restart. Exported as
  `warmup-rolling-failed.*`; ZIP tested.
- `load-f7d0ceaf6604`: warmup stopped after 15 admissions, stale samples again.
  Subsequently db3 group 100.99.0.161–180 became unreachable. At this checkpoint
  there are 6 confirmed, 3 settling, 4 proving, 2 unknown transactions. This is
  a live snapshot, not a terminal accounting statement.
- Unknown `tx-308e029f6662` and `tx-d4bbfb4d7a7a` report receiver SSH connection
  failures to .168/.169. Do not replay, revert payer state, delete journals, or
  release reservations without canonical-state investigation.

Route to .161 uses ens19 / 100.99.0.99; neighbor resolution is FAILED. Ping to
.161 on either interface failed while .123 replied in 0.23 ms. The user has
been asked to check db3 host/LXC/network without resetting containers/accounts.
No host reboot, network route change, account reset or node upgrade was performed.

## Changes and evidence

`bfc120b`: monitor slots advance independently instead of waiting for each batch;
oldest observations go first. This improves queue behavior but **did not by
itself resolve** stale samples. `--prepared-run` adds guarded new measurement
admission without replaying completed account preparation.

`f49d959`: immutable decoded store snapshots, copied before callbacks, avoid
repeated full JSON decoding. Live 6.5 MB state benchmark: JSON decode 113 ms,
clone 19 ms; durable update approximately 252 ms. Backup/fsync semantics remain.
Full Lab race suite passed. Controller PID 1583073 uses this change and the rolling
monitor; its log is `controller-snapshot.log`. Node processes were not restarted.

Further inspection found GetAccountState scans the entire chain backwards on
every request to locate the current commitment. Read-only probes: block query
around 0.36 seconds; account query around 4 seconds, another exceeded 10 seconds.
The pending node fix caches a commitment location and checks its canonical block
hash before reuse. Negative results validate the scanned canonical head and scan
only new blocks; reorgs/changed commitments invalidate hints. Read/cancellation
errors cannot populate a partial cache. Tests cover those cases and pass with
`go test -race -vet=off -tags generic .../internal/ethapi -run TestCommitmentLocation`.
No circuit, pk/vk, proof verification or private-account transition rule changes.

Ubuntu22-compatible runtime build is still running, log `query-cache-build.log`;
the node-query fix has **not** been deployed or live-validated. Do not treat the
existing dist archive as the new artifact until the build finishes and SHA/key
checks pass. No automatic retry runner remains active after the invalid warmup.

## Required continuation

Restore db3 connectivity, reconcile all in-flight/unknown transactions without
replay, finish and validate the compatible runtime, then upgrade only at a safe
checkpoint using preserved directories/journals. Recheck all 100 node versions,
roles, connectivity, account readiness and sampling latency before admitting a
new warmup. Only a full valid 3600-second window plus draining, run/experiment
exports and verified shutdown constitutes completion. The external network
failure currently prevents that outcome.
