# Fresh 100-node pv5 replacement experiment — deployment in progress

The user authorized a fresh independent run after read-only checks found all
20 replacement containers (node slots 63–82) reachable but without the old
`/opt/pfap-lab` runtime/account/chain directory. Existing unknown transactions
cannot be recovered merely by initializing these new containers.

Old experiment `exp-05406ae40c07` was exported as JSON/HTML/CSV ZIP
(`preserved-before-pv5.*` under `lab/data/hour-100-durable.LVuLdv`). ZIP validation
passed. Its 100 stop inspections completed with status `stopped`. Old records,
disk directories where present, and unknown transaction reservations remain;
no replay, rollback, deletion or initialization in old directories was performed.

New experiment: `exp-c17eebe55019`, created September 10 around 15:13 +08:00.

- 100 distinct placements, one process per server. All 100 passed strict SSH,
  no-geth-process and root-disk-space preflight before creation; the controller
  additionally applies its runtime/DAG disk preflight during deployment.
- Five miners on configured host groups pv9, db2, db1, pv5, pv4; one local
  observer; 94 traders. Group labels follow the user's replacement configuration.
- New network 55673, P2P base 34000, RPC base 44000, full mesh.
- Runtime SHA-256 `700f4f244f57a0b5ab6e9a2f8d5ba76229b0c6958ce958584989f55d9a1e6e42`,
  Ubuntu22-compatible build including canonical commitment location caching.
  All eight pk/vk files were compared against the old deployed 85e1e4… runtime
  and are byte-identical. No new keys were generated.
- Fresh preparation, minimum 600-second warmup, separate 3600-second formal
  Transfer measurement, drain, run/experiment JSON/HTML/CSV exports and stop.

Evidence directory: `lab/data/hour-100-pv5.daqJUg/`. `preflight.json` records
server checks; `created.json` records the configuration; `progress.log` records
the active one-shot runner. `before-switch-private-state.json` is sensitive and
must not be published. The launch and run markers prevent accidental duplicate
execution. Do not rerun initialization after an ambiguous write or unknown tx.

The runner `lab/scripts/run-live-hour.cjs` has been launched for this exact ID
and SHA. As of this checkpoint deployment is underway; there is no completed
performance window. The node-query fix has unit/race coverage but its effect
under full live load still requires verification. Do not report the experiment
as successful before the complete-window and export checks actually pass.
