# Completed preliminary low-concurrency baseline — 2026-09-10

Run `load-79eb4bd575e8` in experiment `exp-c17eebe55019` completed with
`completeWindow=true`. Measurement was 22:57:56–23:02:56 UTC+08:00 (300 seconds);
draining completed before the 23:05 export. This is not the outstanding 94-trader
high-load one-hour experiment.

## Configuration and result

- Existing 100-node full-mesh network, five dedicated miners, one local observer.
- Four selected traders: node 3, 24, 44, 64 across pv9/db2/db1/pv5; at most two
  disjoint in-flight Transfer pairs, value 1, saturation mode, six confirmations.
- Warmup minimum 120 seconds plus participation/readiness checks. First admission
  received a definitive HTTP 409 due to a node lock; no run was created. Only that
  known rejection was retried, without replaying any transaction.
- Two warmup Transfers and four measurement Transfers all confirmed and reached
  account readiness; zero failed, pending or unknown measurement transactions.
- No invalid-window error. New submissions ended at the measurement deadline;
  the final transactions were allowed to drain beyond the window.

| Metric | Result | Samples / scope |
| --- | ---: | --- |
| Measured submissions, eventual success | 4 / 4 | Measurement cohort, including drain |
| Confirmations observed inside window | 2 | Controller receipt observation time |
| Observed confirmation throughput | 0.00667 TPS | 2 / 300 s; not network capacity |
| Mean admission-to-receipt latency | 119.24 s | 4; includes proof/queue time |
| Mean broadcast-to-receipt latency | 21.39 s | 4; includes polling error |
| Observed canonical blocks | 31 | Depth-confirmed observer samples |
| Mean block interval | 9.97 s | 30 timestamp intervals |
| Mean encoded block size | 666.35 B | 31 blocks, including empty blocks |
| Mean nonempty block size | 3497 B | Only 1 nonempty observed block |
| Mean execution/state validation | 4.66 ms | 31; excludes consensus-header/network/DB commit |
| Mean payer proof generation | 30.85 s | 4 |
| Mean receiver proof generation | 31.69 s | 4 |

The tiny transaction sample and predominantly empty blocks prohibit a capacity,
statistical-stationarity, long-term reliability or high-load conclusion. This
validates a complete short measurement at two-pair concurrency and demonstrates
successful account reuse after confirmation. Higher concurrency still requires
separate controlled validation; earlier sampling failures are not resolved merely
because this smaller run passed.

## Artifacts and disposition

Evidence: `lab/data/preliminary-100.vaYqPU/`:
`run.html`, `run.json`, `run.zip`, `experiment.html`, `experiment.json`,
`experiment.zip`, `run-config.json`, `progress.log`. Both ZIPs passed
`python3 -m zipfile -t`. Reports contain definitions and measurement limitations.
The whole-experiment export also retains preparation and the earlier invalid
warmup, so it must not be substituted for this run-specific baseline report.

Automatic transaction generation has ended. The network remains running for
the next controlled test; no container, account, chain or host was reset.
