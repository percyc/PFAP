# 100-node one-hour experiment, 2026-09-09

## Network recovered; blocked by private-account recovery — no one-hour result

### Latest checkpoint: 2026-09-09 13:20 +08:00

After the user restarted db3, SSH became reachable but geth IPC refused
connections. All 20 db3 geth processes were recovered from their original
directories, without initialization, transaction replay or account rollback.
All 100 nodes now report running; all five configured miners are mining.

Recovery initially starved behind periodic log rotation. Commit `4edf272`
queues recovery behind the current bounded maintenance lease and prevents
subsequent rotations from overtaking it, with a regression test. The full Lab
race suite passed. Controller-only restart at a checkpoint with no executing
transactions or active workloads activated both this fix and `de8b25c` monitor
lanes. Current controller PID is 1241424 (`lab/data/server.pid`); binary and
sensitive state backups are in `hour-100.TzOXzf`. Existing unknown transaction
statuses/hashes were preserved; subsequent reconciliation updates are distinct
from the restart. No live load was resumed.

The serial full-mesh reconnect path also exceeded its three-minute deadline for
some nodes. Subsequent actual IPC/chain samples confirmed all processes running;
historical recovery errors are not evidence of current process failure. Miner
63 was explicitly resumed using `miner.start(1)` after its reconnect timeout.

Observer receipt checks found successful canonical transactions at blocks 6749
(`tx-7ba406c10ffe`) and 6752 (`tx-d67f089a31df`). Reconciliation moves these into
settling, not automatically reusable accounts. In particular, node 65 still
reports balance 1000000 instead of the expected 1000001. Seven no-hash Transfers
remain unknown; their payer nodes 68/70/72/74/76/78/80 have commitment-readiness
errors. A read-only scan of blocks 6700–7000 found no transactions from their
receiver addresses, but this bounded negative observation does not authorize
replay or rollback.

At 13:20:21, `tx-d67f089a31df` passed the existing two-party readiness check and
became confirmed with ReadyAt. Current totals are 30 confirmed Transfers,
1 settling and 7 unknown. This state/receipt check does not establish recovery
of the original random AccountSK or authorize a new proof from a rebooted account.
Both diagnostic ZIP exports passed `python3 -m zipfile -t` integrity checks.

Source inspection confirms that CreateAccount generates a random AccountSK and
retains it only in memory; the SN startup reader restores sequence fields but
not this key. An existing legacy fallback is not equivalent to restoring the
original random secret. Thus reading SN and reconnecting peers does **not** prove
safe continuation of the rebooted private accounts. Neither missing key material
nor a stale private state should be fabricated from public chain data. A proper
durable account/transaction recovery design and a separately authorized fresh
validation path are needed before declaring restart reliability or running a
valid 94-trader one-hour result.

Diagnostic (not successful-performance) exports were saved as
`interrupted-run.{json,html,zip}` and `interrupted-experiment.{json,html,zip}` in
the evidence directory. They are snapshots while reconciliation is ongoing.
Original experiment/unknown accounts remain preserved and the network is left
running for read-only reconciliation; there is no active automatic workload.

User requested the complete 100-node, one-process-per-server, one-hour experiment.
Fresh experiment: `exp-3b7637ce1061`, network ID 55670, full mesh, P2P base 31000,
RPC base 41000. Runtime SHA256:
`c3933f1bd7be12d3b9b1cb96ae92547e9c5a338a66186599ef05d8e72e509070`.
No keys regenerated. All 100 servers passed SSH/process/disk preflight, with no
running geth process before deployment. All 100 nodes subsequently had 99 peers.

Roles: node 1 (controller-local) independent observer; dedicated miners are
nodes 2/23/43/63/83 (worker-100, db2-01, db1-01, db3-01, pv4-01), one per physical
host group. Remaining 94 nodes are traders. This is a shared LXC-host experiment,
not 100 physically isolated machines. Miners do not submit anonymous transactions.

Preparation: Public funding 0.01 ETH per trader, CreateAccount, then serial Mint
of private balance 1,000,000. Five disjoint Public/CreateAccount jobs at a time;
Mint is globally serial because its legacy mutable witness is not upgraded.
Each step waits for all 100 nodes to observe six-block depth. No uncertain write
is replayed. All 94 Public, 94 CreateAccount and 94 Mint transactions succeeded:
282 confirmed preparation transactions with zero errors. Run
`load-4397ac007aa4` started warmup (2026-09-09 07:05 +08:00), but safely
stopped admission at approximately 07:10 because idle nodes 89–96 had stale
monitor samples. The formal one-hour window never started. This is an invalid
warmup, not a performance result.

Planned run: ready-pool saturation, Transfer value 1, all 94 traders, minimum
600-second warmup with account participation/readiness checks, 3600-second formal
window, six-block confirmation depth, then drain/export/stop. Preparation time is
not included in the measurement window. Successful warmup alone does not prove
statistical stationarity; final trends/sample coverage still need review.

## Controller monitoring fix and safe preparation resume

Source commits: `ce0022d` protocol/native fix, `ec7104c` one-shot runner,
`1509f6d` batched monitoring persistence and guarded preparation resume.
At 65 confirmed Public funding transactions, the runner was paused and terminated
at a clean checkpoint. Node sampling was already concurrent, but one durable
whole-state JSON save/backup per observation caused sample ages above 140 seconds.
The controller now commits successful observations in atomic batches of up to 8,
preserving actual observation timestamps, obsolete-sample guards, immediate manual
queries and existing error handling. No freshness threshold was relaxed.
Batching tests plus Lab `go test -race ./...` passed. After restart, observed maximum
sample ages were approximately 57–73 seconds, all 100 below 90 seconds at checks.

Only the controller restarted; no geth was restarted or state rolled back. The
65 confirmed funding records were explicitly matched and rechecked before skipping
their submissions. Transaction count stayed 65 during this check. No duplicate
payments were generated. Old frozen accounts/failed experiments remain unchanged.

## Warmup findings and external blocker (07:21 +08:00)

The warmup admitted 38 Transfers. A second scheduling issue was found: private
account-state RPCs on proving nodes can wait behind the proof's account lock;
those jobs delay ordinary idle-node sampling even with batched persistence.
The new controller source separates proving nodes into an independent lane
(concurrency 2) while ordinary nodes retain concurrency 8. It rechecks lane
membership before each RPC to handle admissions after queue construction.
Submitted/settling/uncertain nodes remain in ordinary monitoring for reconciliation.
Actual sample timestamps, stale guards, and the two-minute readiness threshold
are unchanged. Tests cover lane classification, isolation and bounded batches;
the full Lab race suite passes. The binary is built but **not activated** while
transactions remain in flight; live effectiveness is not yet validated.

Separately, the whole db3 group (nodes 63–82, 100.99.0.161–180) lost connectivity
after last successful samples around 07:12. Controller SSH reports `No route to
host`, direct ping reports `Destination Host Unreachable`, and neighbor discovery
cannot resolve 100.99.0.161. Other host groups remain reachable. At 07:21 there
were 80 reachable nodes and Transfer counts were 29 confirmed, 2 submitted,
7 proving, with no recorded transaction error yet. This does not prove that the
remaining nine succeeded or that the disconnected processes are still alive.

No account rollback, proof replay, geth restart, or forced transaction-state
rewrite was performed. The controller remains running to reconcile pending
transactions if connectivity returns. User assistance is needed to restore db3
or provide its management address; its shutdown/root cause cannot be inferred
from missing ARP alone. After recovery, verify all 38 outcomes and ReadyAt, then
activate the monitoring fix at a clean checkpoint and create a **new** 600-second
minimum warmup + 3600-second run using the funded accounts. Keep this failed run
separate; do not blindly rerun the one-shot preparation runner.

## Live operation and evidence

- Runner: `lab/scripts/run-live-hour.cjs`, guarded one-shot / confirmed-preparation resume.
- Evidence: `lab/data/hour-100.TzOXzf/` (ignored runtime data).
- Logs: `progress.log`, `progress-resumed.log`, `controller.log`.
- Controller PID is in `lab/data/server.pid` (1013899 at this update; verify before signals).
- Previous controller binary and sensitive state backup are in the evidence folder;
  do not publish those backups or credentials.
- `run-id.txt` appears when the automatic run is actually created.
- The runner writes run/experiment JSON, HTML, CSV ZIP after success, then requests
  and verifies all-node stop. A HALTED log is not success: inspect persisted state
  and pending transactions before any intervention. Do not blindly restart it.

Final window validity, metrics, export integrity, and stop verification remain to
be recorded after completion. Do not label the ongoing preparation as a completed
one-hour experiment.
